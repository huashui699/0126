# 0126 Football 项目基线核对报告

> 核对日期：2026-08-06<br>
> 核对范围：本地代码、Supabase、GitHub、Vercel、项目文档<br>
> 核对方式：只读线上验证 + 本地质量门禁；未修改生产数据或线上配置

## 1. 执行结论

当前 V1 新闻展示链路可以正常运行：代码可构建、Supabase 可匿名只读、GitHub 主分支已合并、Vercel 生产部署成功、生产首页能够展示数据库中的两条新闻。

项目目前处于“基础展示版已上线、目标 MVP 尚未开发”的阶段。没有阻断现有网站运行的故障；主要风险集中在开发分支治理、故障可观测性、自动化测试，以及球队/来源/日历/AI 可信度等目标能力尚未落地。

| 核对域 | 状态 | 结论 |
|---|---|---|
| 本地代码 | 绿色 | ESLint、TypeScript、Next.js 生产构建全部通过 |
| Supabase | 绿色 | RLS 已启用；匿名角色仅可读取；线上读取返回 2 条数据 |
| GitHub | 绿色 | PR #1 已合并至 `main`；合并提交为 `7392795` |
| Vercel | 绿色 | 主分支部署检查成功；生产域名 HTTP 200 |
| 项目治理 | 黄色 | 本地仍停留在功能分支，新增项目文档尚未纳入版本控制 |
| 目标 MVP 能力 | 黄色 | 当前只有新闻列表，球队关注、日历、采集、AI 可信度尚待开发 |

状态定义：绿色 = 正常且满足当前基线；黄色 = 不阻断当前运行，但应纳入近期任务；红色 = 阻断运行或存在高风险。本次未发现红色项。

## 2. 核对证据

### 2.1 本地代码与构建

- 技术栈：Next.js `16.3.0`、React `19.2.8`、TypeScript `6.0.2`、Supabase JS `2.112.1`。
- `eslint .`：通过，0 错误。
- `tsc --noEmit`：通过，0 类型错误。
- `next build`：通过；首页成功预渲染，重新验证周期为 5 分钟。
- `git diff --check`：通过，未发现空白符错误。
- 源代码和 README 均为正常 UTF-8 中文；此前 PowerShell 中的乱码属于默认读取编码问题，不是文件损坏。
- `.env.local` 已被 `.gitignore` 排除；仓库仅提供不含真实凭据的 `.env.example`。

### 2.2 Supabase

- migration 已创建 `public.news`、时间倒序索引和 2 条种子数据。
- `public.news` 已启用 RLS。
- `anon` 与 `authenticated` 仅获得 `select`；`insert/update/delete` 已显式撤销。
- 前端仅使用 Publishable key，没有发现 `service_role` 或 secret key 暴露。
- 线上 Data API 只读测试成功，返回：
  - `皇马开启新赛季备战`
  - `曼城公布新赛季计划`
- 近期 Supabase 变更中，与当前托管项目直接相关的已知项不构成阻塞；项目已使用 TypeScript 6，也满足未来 `supabase-js` 对 TypeScript 5+ 的要求。

### 2.3 GitHub

- 远程仓库：`huashui699/0126`。
- PR #1 状态：`MERGED`，合并时间 `2026-08-06T12:37:05Z`。
- `origin/main`：`7392795e8986e8be97a74e48c5037be1e47837d4`。
- GitHub 上的 Vercel 提交状态为 `success`。
- 当前本地功能分支与 `origin/main` 的已跟踪文件内容一致，无代码差异。

### 2.4 Vercel 生产环境

- 生产地址：<https://0126football.vercel.app>
- HTTP 状态：`200`。
- 页面包含站点标题、Supabase 已连接标识，以及两条线上新闻。
- 主分支提交对应的 Vercel 部署状态为 `Deployment has completed`。

### 2.5 项目文档

本地已有以下规划文档：

- `PROJECT_PLAN.md`：产品目标、范围与架构。
- `SOURCE_REGISTRY.md`：球队与媒体来源清单。
- `DELIVERY_CALENDAR.md`：45 个工作日的每日交付计划。

上述文档当前位于未跟踪的 `docs/` 目录，尚未提交到 GitHub，因此暂时不属于远程项目基线。

## 3. 风险与阻塞清单

| ID | 等级 | 风险/缺口 | 影响 | 建议处理 |
|---|---|---|---|---|
| R-01 | 中 | 本地仍在 `agent/initial-football-mvp`，不是 `main` | 后续开发可能基于错误分支继续，增加合并混乱 | 下一次实施前切回并同步 `main`，再建立 `codex/` 功能分支 |
| R-02 | 中 | `docs/` 尚未纳入 Git | 项目目标、来源和排期无法在团队间形成统一基线 | 审阅后提交文档并通过 PR 合并 |
| R-03 | 中 | 数据读取失败或空表时自动展示预览数据 | 生产故障可能被“看起来仍有内容”掩盖 | 区分空数据和请求失败；增加明确告警与监控 |
| R-04 | 中 | 没有自动化单元、集成或 E2E 测试 | 后续快速迭代时回归风险上升 | 从球队关注和日历主流程开始建立测试 |
| R-05 | 中 | 尚无生产可观测性基线 | API、数据库或采集失败不易及时发现 | 配置 Vercel/Supabase 日志检查、错误告警和健康检查 |
| R-06 | 中 | 目标数据模型尚未建立 | 无法支持 14 队、来源、关注、日历、事件聚类和可信度历史 | 按 Day 6–10 计划通过 migration 扩展 |
| R-07 | 高 | Day 3 审核未发现可直接用于生产自动采集的首批来源 | 真实来源 Adapter 和连续运行验收依赖商业授权或官方 API | 优先联系 BBC、kicker、Gazzetta、俱乐部与联赛；框架先用模拟 Feed 开发 |
| R-08 | 中 | 可信度规范已冻结，但规则引擎、人工后台和 300 条评测集尚未实现 | 若实现偏离规范，仍可能误标绿色或公开敏感内容 | Day 35–40 按规范实现硬规则、双人复核和自动评测 |
| R-09 | 低 | 缺少 `loading.tsx`、`error.tsx` 等页面级状态 | 网络抖动或运行异常时用户反馈不足 | 在日历/筛选页面开发前补齐统一状态组件 |

当前阻塞项：无。R-01 与 R-02 应在下一次代码实施前处理，但不影响现有生产网站。

## 4. 与目标 MVP 的差距

| 能力 | 当前状态 |
|---|---|
| 新闻列表与响应式首页 | 已完成基础版 |
| Supabase 只读数据 | 已完成基础版 |
| 14 支球队目录与球队选择 | 未开始 |
| 用户关注持久化 | 未开始 |
| 日历与日期筛选 | 未开始 |
| 官方/媒体/RSS 自动采集 | 未开始 |
| 社交平台来源接入 | 未开始，且需单独授权/条款审核 |
| AI 中文摘要、去重和球队映射 | 未开始 |
| 绿/蓝/红可信度与判定理由 | 未开始 |
| 人工审核后台 | 未开始 |
| 监控、告警与采集运行记录 | 未开始 |

## 5. 基线验收与下一步

Day 1 的技术核对已经完成，现有 V1 可作为后续开发基线。项目看板尚未接入独立工具，本报告中的风险表先作为风险台账与阻塞清单。

建议下一步按日历推进 Day 2：完成球队选择、日历、信息详情和可信度解释的用户流程，输出页面清单、核心用户故事和低保真流程图。在开始代码改动前，先处理 R-01（同步主分支）和 R-02（将审阅后的文档纳入 Git）。

