# Day 43 发布测试计划与首轮基线

> 启动日期：2026-08-09  
> 当前状态：2026-08-25 P0 双端回归与真实 Supabase 只读 smoke 已通过；300 条标注工作区、发现接口 5xx 恢复与关键语义增量验证通过；月历高数据量、性能、完整无障碍与远端恢复证据未完成

## 2026-08-25 P0 稳定化复核

- 修复 Supabase 查询故障时的详情降级：列表使用验收 fixture 后，fixture 详情同样可读；查询成功但确实不存在的 ID 仍进入明确空状态。
- 默认日期口径固定为“当月最新有内容日期”，测试同步为 8 月 13 日，不再保留与实现冲突的 8 月 6 日断言。
- Playwright 确定性回归：桌面 Edge 14/14、Pixel 7 模拟移动端 14/14，共 28/28 通过。
- 使用现有 `.env.local` 本地直连真实 Supabase 完成只读 smoke：桌面/移动共 4/4，通过真实模式识别、新闻列表、详情原文和不存在 ID 空状态。
- Vercel Preview 已创建并达到 `READY`：deployment `dpl_83NVdNvmrzHGr4aWQecDVGXRN6FA`，URL `https://0126football-reuqta46w-oprjirofill-4904s-projects.vercel.app`；远端 Next.js 16.3.0 构建通过，13 个路由完成生成。
- Preview 内容受 Vercel SSO 保护；普通请求收到预期的 302 SSO 跳转，但本机 `vercel curl` 两次连接超时，应用内浏览器访问亦被安全策略拒绝。部署最近 30 分钟无应用请求/错误日志，说明请求没有越过保护层；因此本轮保留本地真实 Supabase 4/4 作为数据路径证据，正式 Preview 交互与回滚证据仍待具备受保护访问能力的环境补录。
- Windows 下 Playwright 自管 WebServer 清理问题已通过 `scripts/run-e2e.mjs` 关闭：运行器先确认测试端口空闲，只管理自己启动的 Next 进程，并在结束后按已验证 PID 清理；全量 28/28 复跑已以退出码 0 干净收尾。

## 首轮基线

- ESLint：通过。
- TypeScript：通过。
- ingestion 单元测试：8/8 通过。
- intelligence 单元测试：11/11 通过，包含 Day 41 人工评测门禁。
- Next.js 16.3.0 生产构建：通过，12 个路由完成构建。
- Playwright：桌面 Edge 7/7、Pixel 7 模拟移动端 7/7，核心月历、日期摘要、球队选择、本地保留、详情返回、高级筛选和延期路由均通过。

本次 E2E 无法连接远端 Supabase，应用按设计降级为明确标记的 Preview fixture。14 条用例均通过，但测试进程在 Windows WebServer 清理阶段未自动退出，最终由 180 秒外层时限终止。因此该结果证明核心交互通过，不证明远端数据路径或测试运行器能干净收尾。

## 2026-08-09 增量验证

- 标注台桌面 Edge 5/5、Pixel 7 模拟移动端 5/5 通过；使用已运行的本地服务作为 `E2E_BASE_URL`，两轮均以退出码 0 干净收尾。
- 300 条本地标注样本可在刷新后恢复，并可检索、选择第 300 条记录；这证明 Day 41 工作区目标容量可操作，不等同于月历 10,000 条压力门禁。
- 自动来源发现首次返回 503 后，界面显示明确失败状态、按钮恢复可用，第二次请求成功呈现候选。
- 标注台主标题、自动发现区域、阶段页签和删除操作具备可识别名称；键盘 Enter 可切换到 B 复核阶段。
- 本轮是关键路径自动检查，不替代 WCAG 2.2 AA 自动扫描、对比度审计和完整人工键盘记录。

## 待执行矩阵

| 维度 | 场景 | 通过条件 |
|---|---|---|
| 高数据量 | 单月 10,000 条、单日 500 条、14 队全选和 5 队筛选 | 无崩溃、无重复请求风暴；交互延迟与查询计划可接受 |
| 移动端 | 360×800、Pixel 7、横竖屏、200% 字号 | 无横向溢出；球队选择、筛选、日期列表、详情均可操作 |
| 无障碍 | 键盘、焦点、语义标题、名称/角色、对比度、颜色非唯一 | 阻塞级 WCAG 2.2 AA 问题为 0 |
| 4G 性能 | 冷启动、慢 4G、无缓存首页与详情 | 记录 LCP/INP/CLS 和服务端响应；达到团队确认的发布预算 |
| 恢复 | Supabase 超时/5xx、空数据、无效月份、详情不存在 | 显式降级或错误状态；页面不白屏、不泄露内部错误 |
| 后台 | 未授权、弱密钥、错误 UUID、超长输入、绿色硬门禁 | 全部拒绝；不返回数据库详情；审计记录完整 |

## 下一步证据

1. 在可访问 Preview Supabase 的环境重跑 14 条 E2E，确认不是 fixture 路径。
2. 准备分层高数据量测试数据；数据必须明确隔离，测试后可回滚。
3. 接入自动无障碍扫描并保留完整键盘人工检查记录；当前仅完成标注台关键语义和阶段切换基线。
4. 使用统一移动网络配置采集性能结果，先冻结预算，再判定通过/失败。
5. **已完成（2026-08-25）**：隔离 Windows 下 Playwright WebServer 清理超时，`pnpm test:e2e` 由项目运行器管理测试服务器并返回真实测试退出码。
6. 按 [Day 44 Preview 回放与回滚预演手册](./DAY44_PREVIEW_REPLAY_RUNBOOK_2026-08-09.md)准备演练；在 Day 41–43 门禁通过前不启动连续 7 天计时。
