# M7 聚类、可信度与运营复核评审

> 评审日期：2026-08-08  
> 覆盖：Day 36–40  
> 结论：工程实现与权限门禁通过；真实数据准确率留待 Day 41

## 已交付

- URL 规范化与标题 Jaccard 相似度聚类，输出方法、分数和说明。
- 引用链、同一注册来源、同一规范域名均不计为独立证据。
- 绿/蓝/红规则引擎；绿色必须同时满足：官方来源、身份已验证、采集已批准、直接官方声明、非模拟数据。
- 数据库再次执行同一绿色硬门禁，避免 API 或 AI 绕过规则。
- `source_citations`、`reliability_assessments`、`trust_status_history`、`review_actions` 与可撤销逻辑合并。
- 日历高级类型/可信度筛选写入 URL 并可刷新恢复；详情页读取受 RLS 保护的净化状态历史。
- `/admin/review` 运营台与管理员 API：改标签、隐藏/恢复、合并事件簇、查看审计和撤销。

## 权限与远端烟雾测试

- 5 张内部 AI/可信度表均启用 RLS；匿名/普通用户不能读取作业、引用图、评估或操作者身份。
- 管理函数仅授予 `service_role`；公开历史改为 `security_invoker` 视图和列级授权。
- 回滚式远端测试确认：模拟内容升级绿色会被数据库拒绝；隐藏操作可撤销；撤销记录追加而非删除历史。
- 范围仍是 14 队、0 个启用真实来源；没有借 Day 31–40 绕过授权门禁。
- 桌面与移动 Playwright 共 14/14 通过；浏览器视觉检查确认日历、高级筛选入口和复核台均正常渲染，无错误覆盖层。

## 尚待 Day 41 的质量证据

- 真实人工标注数据上的聚类 precision ≥ 90%。
- 300 条人工集上的可信度一致率 ≥ 85% 和绿色 precision = 100%。
- 运营责任人、管理员密钥轮换和双人复核流程的组织验收。

这些是质量/运营上线门禁，不影响 Day 36–40 工程闭环已经完成，但会阻止 M7 被表述为生产数据质量已验证。
